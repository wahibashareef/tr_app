import 'package:flutter/material.dart';

class SaleTemplate extends StatelessWidget {
  const SaleTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}